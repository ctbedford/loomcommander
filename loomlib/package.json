{
  "name": "loomlib",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "generate:seed": "npx tsx scripts/generate-seed.ts",
    "predev": "npm run generate:seed",
    "dev": "vite",
    "prebuild": "npm run generate:seed",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "d3-bboxCollide": "^1.0.4",
    "d3-force": "^3.0.0",
    "umap-js": "^1.4.0"
  },
  "devDependencies": {
    "@types/d3-force": "^3.0.10",
    "fake-indexeddb": "^6.0.0",
    "gray-matter": "^4.0.3",
    "marked": "^17.0.1",
    "tsx": "^4.21.0",
    "typescript": "^5.3.3",
    "vite": "^5.4.2",
    "vitest": "^2.0.5"
  }
}
